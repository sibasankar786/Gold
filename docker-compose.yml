version: "3.9"

services:
  mongodb:
    image: mongo:7.0
    container_name: astratrade_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: astratrade

volumes:
  mongo_data:
